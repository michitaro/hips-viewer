type Properties = { [key: string]: string }

const hipsInformation = {
    show: true,
    properties: null as null | Properties,
    properties_url: null as null | string,
}

export { hipsInformation, Properties }